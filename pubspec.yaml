name: unreal_space
description: Test Project for AR with Flutter

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  arcore_flutter_plugin:
    git:
      url: https://github.com/Haris2v/arcore_flutter_plugin
  vector_math: ^2.1.4
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/earth.jpeg